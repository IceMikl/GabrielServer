version: '3.3'
services:

  gabriel_request_handler:
    build: request_handler/
    #image: icemikl/request_handler:latest
    container_name: request_handler_container
    depends_on:
      - gabriel_db
      - adminer
    restart: always
    ports:
      - 8080:8080
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=password
      - POSTGRES_HOST=gabriel_db
      - POSTGRES_PORT=5432
      - POSTGRES_DB=gabriel_db
      - BASIC_AUTH_USERNAME=user
      - BASIC_AUTH_PASSWORD=gabriel_user
      - BASIC_AUTH_FORCE=True


  gabriel_db_manager:
    build: db_manager/
    #image: icemikl/db_manager:latest
    container_name: db_manager_container
    depends_on:
      - gabriel_db
      - adminer
    restart: always
    ports:
    - 8087:8087
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=password
      - POSTGRES_HOST=gabriel_db
      - POSTGRES_PORT=5432
      - POSTGRES_DB=gabriel_db
      - BASIC_AUTH_USERNAME=user
      - BASIC_AUTH_PASSWORD=gabriel_user
      - BASIC_AUTH_FORCE=True


  gabriel_db:
    build: gabriel_db/
    #image: icemikl/gabriel_db:latest
    container_name: gabriel_db_container
    restart: always
    ports:
      - 5432:5432
    #volumes:
    #  - $HOME/docker/volumes/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=gabriel_db


  adminer:
    image: adminer
    container_name: adminer
    restart: always
    ports:
      - 8888:8080

